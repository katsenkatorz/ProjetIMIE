{% extends "UserBundle::layout.html.twig" %}

{% block title %}{{ 'UserAccount' }}{% endblock %}

{% block content %}
    <div id="content">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                <div style="margin-top: 25%;" class="panel panel-default">
                    <div class="panel-heading">
                        Connection
                    </div>
                    <div class="panel-body">
                        <div class="text-center">
                            <a style="width: 90%" class="btn btn-primary" href="{{ path('home_homepage') }}">User</a>
                            <br /><br />
                            <a style="width: 90%" class="btn btn-danger" href="{{ path('admin_homepage') }}">Admin</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
