{% extends "::base.html.twig" %}


{% block content %}
    {% include "AdminBundle:layout:header.html.twig" %}

    {% include "AdminBundle:layout:sidebar.html.twig" %}

<div id="content">
    <div id="content-header">
        <div id="breadcrumb"><a title="" class="tip-bottom"><i class="fa fa-pencil-square-o"></i>Edition de profil</a>
        </div>
    </div>

    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <h2>Édition du profil</h2>
            </div>
            <div class="col-sm-12 col-md-6">
                <hr>
                {% block fos_user_content %}
                    {% include "UserBundle:Group:edit_content.html.twig" %}
                {% endblock fos_user_content %}
            </div>
        </div>
        <div class="row">
            <div class="col-sm-12 col-md-6">
                <hr>
                <p>Cliquez sur le bouton ci-dessous pour être redirigé vers la page de modification du mot de passe.</p>
                <a href="{{ path('fos_user_change_password') }}" class="btn btn-default">Mettre à jour le mot de
                    passe</a>
            </div>
        </div>
    </div>


    {% endblock %}
